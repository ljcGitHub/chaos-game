import THREE from '../../../_libs/three.js'

export const baseMaterial = new THREE.MeshBasicMaterial({ color: 0x00ff00, wireframe: true })

// export const baseMaterial = new THREE.MeshNormalMaterial({ wireframe: false })
